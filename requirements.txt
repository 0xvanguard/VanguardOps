# --- Web framework ---
fastapi>=0.110.0,<0.120.0
uvicorn[standard]>=0.27.0,<0.35.0
python-multipart>=0.0.9

# --- Settings & validation ---
pydantic>=2.6.0,<3.0.0
pydantic-settings>=2.2.0,<3.0.0
email-validator>=2.1.0

# --- Persistence ---
sqlalchemy>=2.0.27,<2.1.0
alembic>=1.13.0,<2.0.0
psycopg2-binary>=2.9.9
# SQLAlchemy 2.0 requires `greenlet` only when using async; we keep sync.

# --- Async tasks ---
celery>=5.3.6,<6.0.0
redis>=5.0.1,<6.0.0

# --- Auth ---
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
pyjwt>=2.8.0,<3.0.0

# --- Observability ---
prometheus-client>=0.20.0
structlog>=24.1.0

# --- Rate limiting & security ---
slowapi>=0.1.9

# --- HTTP client (used by tests and integrations) ---
httpx>=0.27.0,<1.0.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0
pytest-asyncio>=0.23.0
factory-boy>=3.3.0
freezegun>=1.4.0

# --- Lint / format ---
ruff>=0.4.0
